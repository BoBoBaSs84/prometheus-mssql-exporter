# Copy to .env and adjust. Loaded automatically by `npm start` / `npm run dev`.

# Required
SERVER=localhost
USERNAME=SA
PASSWORD=your_Strong_Passw0rd

# Optional
PORT=1433
ENCRYPT=true
TRUST_SERVER_CERTIFICATE=true
EXPOSE=4000
COLLECT_DEFAULT_METRICS=true
DEBUG=app,db,metrics
